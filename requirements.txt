# YAWatch-Luna Development Dependencies
# pip install -r requirements.txt

# Twilio SMS
twilio>=9.0.0,<10.0.0

# Redis (Luna memory)
redis>=5.0.0,<6.0.0

# Async support
aiohttp>=3.9.0,<4.0.0

# WebSocket (OpenAI Realtime API)
websockets>=12.0,<17.0

# HTTP client (SIRET API)
httpx>=0.27.0,<1.0.0

# Utils
python-dateutil>=2.8.0

# Form parsing (requis par FastAPI pour les webhooks Twilio/Stripe)
python-multipart>=0.0.6

# Auth (multi-tenant)
PyJWT>=2.8.0
bcrypt>=4.0.0

# Anthropic Claude (chat écrit + luna-brain)
anthropic>=0.69.0,<1.0.0

# System monitoring (Cortex)
psutil>=5.9.0,<7.0.0

# PDF generation (audit reports)
fpdf2>=2.7.0,<3.0.0

# Computer vision / perception (aide contextuelle)
ultralytics>=8.0.0,<9.0.0
opencv-python-headless>=4.8.0,<5.0.0

# Form Filler (remplissage PDF)
PyMuPDF>=1.25.0
Pillow>=11.0.0

# Monitoring & Error tracking
sentry-sdk[fastapi]>=2.0.0,<3.0.0
