# YAWatch-Luna Cloud Run Dependencies (allege, sans perception camera)
# pip install -r requirements-cloudrun.txt

# Web framework
uvicorn[standard]>=0.30.0
fastapi>=0.110.0
starlette>=0.37.0

# Twilio SMS + Voice
twilio>=9.0.0,<10.0.0

# Redis (Luna memory)
redis>=5.0.0,<6.0.0

# Async support
aiohttp>=3.9.0,<4.0.0

# WebSocket (OpenAI Realtime API)
websockets>=12.0,<17.0

# HTTP client
httpx>=0.27.0,<1.0.0

# Utils
python-dateutil>=2.8.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Auth (multi-tenant)
PyJWT>=2.8.0
bcrypt>=4.0.0

# Stripe
stripe>=7.0.0

# Documents (certificat DOCX)
python-docx>=1.0.0

# OpenAI (voix temps réel)
openai>=1.0.0

# Anthropic Claude (chat + luna-brain)
anthropic>=0.69.0,<1.0.0

# Cortex (cerveau autonome)
pyotp>=2.9.0
qrcode[pil]>=7.4.0
psutil>=5.9.0
fpdf2>=2.7.0

# Simli TTS fallback (gratuit si OpenAI TTS echoue)
edge-tts>=6.1.0

# Form Filler (remplissage PDF)
PyMuPDF>=1.25.0
Pillow>=11.0.0

# Monitoring & Error tracking
sentry-sdk[fastapi]>=2.0.0,<3.0.0

# Karaoké — brouillons d'enregistrement (stockage GCS persistant)
google-cloud-storage>=2.16.0,<4.0.0
